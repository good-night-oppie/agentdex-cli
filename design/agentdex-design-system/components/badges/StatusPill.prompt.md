Battle status pill — Pokémon conditions (solid) and health states (outlined). Green=healthy, amber=status, red=fainted.

```jsx
<StatusPill status="PAR" />
<StatusPill status="BRN" />
<StatusPill status="fainted" />
```
