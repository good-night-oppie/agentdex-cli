# .harness/CORPUS_QUERY_KEYWORDS — adx-cli session-start corpus query seeds.
#
# Consumed by ~/gh/harness-engineering/scripts/sessionstart_corpus_query.sh
# (wired as a SessionStart hook in ~/.claude/settings.json). Each line is one
# keyword; `#` comments + blank lines are stripped before query.
#
# Source: created by harness-engineering workflow w2we4t2ef (2026-06-09 propagation
# pass; tmux harness-2 transcript).
# Drift policy: re-diff against ../eddie-agi-kb's CORPUS_QUERY_KEYWORDS schema +
# review the adx-cli keyword set quarterly per .harness/README.md.

agentdex
bridges
oracle
pareto
expedition
fairness
codex
claude code
manus
KAOS
provenance
sealed evaluator
multi-agent review
expedition battle
