# agents/ops — agentdex-cli

## Running
```bash
uv sync
uv run <cli-cmd>            # main entry
uv run python -m pytest    # tests
```

## Env vars (declare here — agent reads, doesn't grep)
- TODO: enumerate required env vars + defaults

## Secrets (manifest, not contents)
- (read from 1Password / SOPS / env — never commit)

## Ports / endpoints
- TODO: list bound ports
